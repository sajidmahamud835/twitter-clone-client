/** @type {import('next').NextConfig} */

// This is the configuration file for the Next.js app.
const nextConfig = {
  reactStrictMode: true, // This is a feature that enables the use of the strict mode when rendering React components.
}

module.exports = nextConfig 